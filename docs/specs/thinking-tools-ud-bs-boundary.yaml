# Seed Spec — unknown-discovery / build-spec 역할 재정의
# Generated by: thinking-tools/build-spec (dogfooding — 이 스펙 자체가 build-spec으로 작성됨)
---
# Output conforms to thinking-tools/reference/common-schema.md
skill: build-spec
schema_version: 1
version: 4.1.0
generated: 2026-07-26
input:
  target: unknown-discovery와 build-spec의 역할 재정의 및 라우팅 정리
  options: []
output:
  type: spec
  structure: templates/SEED_SPEC.yaml

# build-spec extensions
spec_version: 1
created: 2026-07-26
target: unknown-discovery와 build-spec의 역할 재정의 및 라우팅 정리
domain: tech
brownfield: true
refine_generation: 0

goal:
  statement: >-
    unknown-discovery(UD)와 build-spec(BS)의 역할 경계를 description·공유문서·배관 세 층에서
    일치시켜, 아이디어 단계 요청이 UD에 도달하고 UD의 발견이 Seed로 흘러가게 한다.
  clarity_score: 0.9

constraints:
  - id: c1
    type: technical
    description: >-
      두 스킬이 공유하는 계약(축 표 + 세 경로 + findings→blindspots 매핑)은
      thinking-tools/reference/ 아래 단일 파일로 유지한다. 파일을 쪼개지 않는다.
    hard: true
    rationale: >-
      계약 파일이 여러 개로 늘면 이번 범위에서 제외한 드리프트 문제(INTERVIEW_STATE.md 두 벌,
      격리 채점 정책 불일치)를 그대로 재생산한다.

  - id: c2
    type: other
    description: UD 인터뷰 길이(라운드 수·단계 수)가 현재 대비 늘어나면 안 된다.
    hard: true
    rationale: >-
      UD가 회피되는 원인이 체감 무거움이다. 출구와 감지를 붙이면서 라운드가 늘면 개선이
      역효과가 되어, 고치려던 문제를 더 키운다.

  - id: c3
    type: other
    description: >-
      범위 제외 — (a) 격리 채점 정책 드리프트와 INTERVIEW_STATE.md 두 벌 통합 → #432,
      (b) doc-concretize까지 낀 3자 신호 충돌 → #431. 둘 다 #430 머지 후 착수.
    hard: true
    rationale: >-
      둘 다 사용자 체감이 0인 유지보수 부채이고, 격리 채점 수렴은 UD 게이트의 "임박" 정의를
      새로 만들어야 해서 순수 복사가 안 된다. 넣으면 스코프가 "경계 정리"에서
      "두 스킬 리팩터"로 번진다.

  - id: c4
    type: technical
    description: 기존 트리거 문구는 삭제하지 않고 추가만 한다.
    hard: false
    rationale: >-
      thinking-tools/scripts/test/check-trigger-regression.py가 드롭을 CI에서 차단한다.
      규약이 이미 강제되므로 제약으로 다시 세울 필요는 없다.

  - id: c5
    type: technical
    description: plugin.json 버전을 직접 올리지 않는다. description 변경 시 marketplace.json만 동기화.
    hard: false
    rationale: claude-kit은 lockstep 릴리스라 버전은 릴리스 워크플로가 일괄 범프한다.

success_criteria:
  - id: ac1
    description: >-
      샘플 입력 ↔ 기대 스킬 판정 기준표가 공통 reference 파일에 존재하고, 각 항목에
      왜 그 스킬인지가 한 줄로 붙어 있다. 자동 CI 검증은 하지 않는다.
    verifiable: true
    measurable_via: >-
      공통 reference 파일에 기준표가 있는지 확인. 자동화하지 않는 이유를 파일 안에 명시 —
      실제 라우팅은 LLM의 description 매칭이라, 결정적 스크립트는 트리 텍스트에 기대 스킬
      문자열이 있는지만 볼 수 있고 런타임 오라우팅은 통과시킨다. 그런 테스트는 라우팅
      테스트가 아니라 문서 린트이며, 없는 것보다 나쁜 거짓 안전감을 만든다.

  - id: ac2
    description: >-
      UD→BS 왕복이 실제로 1회 성립한다 — 기존 seed 파일을 대상으로 UD를 돌리고, 나온
      Discovery Report를 BS refine mode에 넣어 seed v2(refine_generation: 1)가 산출된다.
    verifiable: true
    measurable_via: docs/specs/ 아래 refine_generation이 1 이상인 seed 파일의 존재

  - id: ac3
    description: >-
      UD를 거쳐 들어온 BS 세션에서 Phase 2.5 blind-spot pass가 건너뛰어지고, STATE에
      blindspot_pass: skipped와 그 사유가 남는다.
    verifiable: true
    measurable_via: build-spec SKILL.md Phase 2.5의 스킵 조건 문구 + ac2 왕복 시 STATE 확인

context:
  existing_stack:
    - thinking-tools 플러그인 (스킬 7개 + thinking-facilitator 에이전트)
    - thinking-tools/scripts/test/check-trigger-regression.py (트리거 드롭 차단)
    - thinking-tools/reference/ (state-contract.md, common-schema.md — 공유 계약 선례)
  dependencies:
    - UD findings[].category 와 Seed blindspots[].area 가 이미 같은 어휘를 공유 (common-schema.md 기인)
    - build-spec Refine mode(A3)의 prior-seed + feedback preamble 주입 경로 — 왕복의 복귀 배관
  integration_points:
    - unknown-discovery/SKILL.md description (Quick Mode 트리거 노출 + 영역 주장 + BS 상호 언급)
    - build-spec/SKILL.md description (아이디어 단계를 흡수하지 않도록 범위 축소)
    - build-spec/SKILL.md:26-32 (현행 MECE 표 — 공통 reference로 이전)
    - build-spec/SKILL.md Phase 2.5 (UD 경유 시 스킵 조건 추가)
    - unknown-discovery/SKILL.md:74 (Phase 0 Glob 패턴 — docs/specs/*.yaml 추가)
    - unknown-discovery/SKILL.md:155-159 + templates/DISCOVERY_REPORT.md (Post-Discovery에 Seed 출구)
    - thinking-tools/reference/{신규 파일} (축 표 + 세 경로 + 매핑 규칙)
    - agents/thinking-facilitator.md:41-45 (부수 수정 — 주 라우팅 지점이 아님)
  backlog_scan: >-
    gh issue list --state open 5건 스캔. 충돌 1건 — #105(dissolve thought-chain, merged
    2026-07-06)가 오케스트레이터 스킬을 제거한 근거가 "부품이 이미 MECE해 오케스트레이션
    니즈 자체가 없다"는 owner 증언인데, 이 작업의 전제(UD-BS는 MECE가 아니다)와 정면 충돌한다.
    처리 — 산출물에 명시하고 #105에 코멘트+백링크, 그리고 이번 다리가 오케스트레이터 스킬이
    아니라 두 스킬 간 직접 경로임을 함께 기록해 재도입 오해를 막는다.
    #113(doc primitive 전용 플러그인 분리)은 다른 축이라 비충돌이며 3자 충돌 이슈와 relates로
    연결한다. #111(build-spec의 thinking-tools 잔류 확정)은 이 전제와 양립한다.

blindspots:
  - area: blind-spots
    question: >-
      thinking-facilitator가 실제 세션에서 호출된 적이 있는가? #105는 thought-chain을
      "텔레메트리 호출 0회"로 지웠는데, facilitator에는 같은 잣대를 대본 적이 없다.
      호출 0회라면 facilitator 자체가 #105와 같은 판정 대상이다.
    note: >-
      Phase 2.5에서 제기되어 부분 확인됨 — history.jsonl의 facilitator 언급 2건은 모두
      "이 파일을 정비하자"는 편집 대상 언급이고 라우팅 수행 기록이 아니다. 이 확인이
      항목 1을 facilitator 트리 수정에서 description 재조정으로 바꾼 근거다.
      facilitator의 존치 여부 자체는 이번 범위 밖.

ambiguity:
  overall: 0.10
  gate_passed: true

clarity_breakdown:
  goal: 0.9
  constraint: 0.9
  success: 0.9
  context: 0.9

metadata:
  interview_rounds: 3
  questions_asked: 7
  generated_by: thinking-tools/build-spec
  source_session: >-
    2026-07-26 — "왜 unknown-discovery 대신 build-spec을 쓰게 되는가"에서 출발한 세션.
    4축(경량 하네스/유지보수/MECE/임팩트) 재검토를 거쳐 이 Seed로 결정화. dogfooding 겸용.
  scoring_isolated: true
  blindspot_pass: done
  dogfooding_note: >-
    이 세션에서 build-spec 자체의 문제 2건이 드러났다. (1) Phase 2의 격리 채점이 Agent
    서브에이전트를 요구하는데 머신 정책이 Agent 호출을 사용자 승인으로 묶고 있어 정면 충돌하며,
    스킬의 fallback이 "티 내지 말고 인라인 채점"이라 사용자가 격리 검증이 빠진 줄 모른 채
    지나간다 → #433. (2) Phase 2.5가 게이트를 통과한 스펙에서 계획을 뒤집는 발견 3건을 냈다 —
    게이트 만장일치 통과 직후였다는 점에서, 게이트가 "묻지 않은 것"에 대해 무력하다는
    스킬 자체의 서술이 실제로 확인되었다. (2)는 스킬이 이미 "게이트 전에는 절대 돌리지 않는다"를
    근거와 함께 명시하고 있어 재고 대상이 아니며, 관찰로만 남긴다.
---
